import ExitToAppIcon from '@mui/icons-material/ExitToApp';
interface Props {
  className?: string;
}
export const LogoutIcon = ({ className }: Props) => (
  <ExitToAppIcon className={className} />
);
