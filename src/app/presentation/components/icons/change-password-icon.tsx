import LockResetIcon from '@mui/icons-material/LockReset';
interface Props {
  className?: string;
}
export const ChangePasswordIcon = ({ className }: Props) => (
  <LockResetIcon className={className} />
);
